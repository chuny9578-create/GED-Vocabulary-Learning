# GED Vocabulary 学习器

这是可发布到 GitHub Pages 的版本。上传到公开 GitHub 仓库后，在 **Settings → Pages** 中将 Source 设为 **GitHub Actions**；每次推送到 `main` 分支都会自动发布。

应用可离线使用。若需要跨设备同步，请先按 `SUPABASE_SCHEMA.sql` 创建同步表，并在 `sync-config.js` 填入 Supabase Project URL 与 anon public key，再提交并发布。
